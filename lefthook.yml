# lefthook.yml — livespec-driver-pi git hook configuration.
#
# Family conventions, scaled to this repo's content. Every `run:`
# field delegates to `just <target>` (the lefthook-must-delegate-to-
# just rule from livespec/SPECIFICATION/non-functional-requirements.md
# §"Toolchain pins").
#
# Authority: livespec/SPECIFICATION/contracts.md
#   §"Pre-commit step ordering" (v219) — pre-commit runs four commands in
#   order (00-install-worktree-pack, 01-lint-autofix-staged,
#   02-commit-pairs-source-and-test, 03-check-pre-commit); commit-msg
#   runs no-commit-on-master then red-green-replay (v034 D3); pre-push
#   runs install-worktree-pack then the full aggregate. The
#   red-green-replay ritual is enforced fleet+adopter-wide (epic
#   livespec-gcp2), regardless of any repo's product-Python footprint.
#
# Why the installer runs FIRST at pre-commit and pre-push: the
# worktree-discipline pack is gitignored-and-installed, so a worktree
# created by raw `git worktree add` (or one predating a
# livespec-dev-tooling pin bump) would otherwise reach the gate with the
# pack absent or stale and fail `worktree_pack_absent` only after the
# whole aggregate ran. The installer is idempotent, writes only ignored
# files, installs from the package this checkout resolves, and the
# byte-identity verifier still asserts the result.
#
# Activation: `just bootstrap` installs the canonical commit-refuse hook by
# REUSING the livespec-dev-tooling installer
# (`python -m livespec_dev_tooling.install_commit_refuse_hooks`, the SINGLE
# source of the structural hook body) as the .git/hooks/pre-commit, pre-push,
# and commit-msg hooks (refuses at the primary checkout; delegates to lefthook
# at secondary worktrees via mise). The commit-msg hook routes argv[1] (the
# commit-message file path) to the red-green-replay stage.

pre-commit:
  commands:
    00-install-worktree-pack:
      run: just install-worktree-pack
    01-lint-autofix-staged:
      run: just lint-autofix-staged
    02-commit-pairs-source-and-test:
      run: just check-commit-pairs-source-and-test
    03-check-pre-commit:
      run: just check-pre-commit

commit-msg:
  commands:
    00-no-commit-on-master:
      run: |
        branch=$(git rev-parse --abbrev-ref HEAD)
        if [ "$branch" = "master" ]; then
          printf 'ERROR: direct commits to master are forbidden; create a feature branch first\n' >&2
          printf 'Hint: git checkout -b <branch-name>\n' >&2
          exit 1
        fi
    01-red-green-replay:
      run: just check-red-green-replay {1}

pre-push:
  commands:
    00-install-worktree-pack:
      run: just install-worktree-pack
    01-check-pre-push:
      run: just check-pre-push
